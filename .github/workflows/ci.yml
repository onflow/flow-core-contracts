name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
      - uses: actions/setup-go@v1
        with:
          go-version: '1.13.10'
      - name: Run tests
        run: export GOPATH=$HOME/go && sudo su - && make ci
