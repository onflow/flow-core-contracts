package test

import (
	"testing"
)

func TestFlowFees(t *testing.T) {
	t.Parallel()

	t.Run("Should be able to change fee factors", func(t *testing.T) {
	})

	t.Run("Should be able to deduct fees", func(t *testing.T) {
	})
}
