// Submit unbonding/unstaking request for the staked tokens through the StakingHelper

transaction (amount: UFix64) {
    // TODO:
    // - get StakingHelper capability
    // - call `unstake` method and pass amount in it
}